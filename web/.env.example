# Clerk — CLERK_SECRET_KEY also backs server-side session verification.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Points Clerk at the embedded sign-in/sign-up pages under app/. Without these
# it redirects to its hosted Account Portal on accounts.dev instead.
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/dashboard
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/dashboard

# Clerk telemetry is collected from development instances only. The NEXT_PUBLIC_
# prefix is required: @clerk/nextjs reads this name server-side and also inlines
# it into the browser bundle.
NEXT_PUBLIC_CLERK_TELEMETRY_DISABLED=1

# Postgres for local dev — the five parts of the connection URL that
# lib/server/databaseUrl.ts reassembles into postgresql://. Kept split to match
# production, where RDS stores its generated credentials as a JSON blob that ECS
# cannot assemble into a URL itself. "Web (frontend + REST API)" in RUNBOOK.md
# starts the container they address.
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=ai_gaussian_splatter
DATABASE_USER=postgres
DATABASE_PASSWORD=test

# S3 — real buckets, not placeholder names. Presigning itself is a local HMAC
# and needs no AWS, but the browser PUTs photos directly to the presigned URL
# and the viewer fetches the .ply the same way, so both buckets must exist and
# carry CORS rules for the local origins. See "Development AWS resources" in
# RUNBOOK.md, which creates the buckets, their CORS rules, and the IAM user
# below.
UPLOADS_BUCKET=ai-gaussian-splatter-dev-uploads
SPLATS_BUCKET=ai-gaussian-splatter-dev-splats
AWS_REGION=us-west-2

# The dev IAM user scoped to those two buckets — replace both with the key pair
# `aws iam create-access-key` returned when RUNBOOK.md created it. Kept here
# rather than in ~/.aws/credentials because `next dev` and the local container
# both load this file, and because the SDK's env provider tests these for
# truthiness: an empty value counts as absent and the chain falls through to
# ~/.aws/credentials, signing upload URLs with whatever profile is configured
# there. The placeholders are non-empty so an unedited .env fails with a 403
# instead of quietly using an admin profile.
AWS_ACCESS_KEY_ID=replace-with-dev-user-key
AWS_SECRET_ACCESS_KEY=replace-with-dev-user-secret

# GPU spot worker (see lib/server/ec2Launcher.ts). Only read when launching a
# job, which local dev never does — but getEnv() runs a single zod safeParse
# over the whole schema and its first caller is the DB pool, so empty values
# here would fail every DB-backed route. Non-empty placeholders keep the
# failure on the launch path, where it means something.
WORKER_AMI_ID=replace-with-worker-ami-id
WORKER_INSTANCE_TYPE=g5.xlarge
WORKER_SUBNET_ID=replace-with-worker-subnet-id
WORKER_SECURITY_GROUP_ID=replace-with-worker-security-group-id
WORKER_INSTANCE_PROFILE_ARN=replace-with-worker-instance-profile-arn

# Abuse protection — all optional, defaults shown
RATE_LIMIT_IP_PER_HOUR=5
RATE_LIMIT_USER_PER_DAY=3
GLOBAL_MAX_JOBS_PER_DAY=20
MIN_PHOTOS_PER_SPLAT=20

# Where the worker PATCHes job status back to. Must be reachable from EC2.
APP_PUBLIC_URL=http://localhost:3000
