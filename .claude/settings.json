{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */web/.env.example) printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"web/.env.example was just edited. Review web/.env and make the same change there unless the divergence is intentional: the two files are kept identical apart from their values.\"}}';; esac; } 2>/dev/null || true",
            "statusMessage": "Checking web/.env sync"
          }
        ]
      }
    ]
  }
}
