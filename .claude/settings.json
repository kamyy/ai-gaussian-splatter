{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in */.env.example) printf '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"Root .env.example was just edited. Review web/.env and worker/.env and make the same change in whichever one(s) the edited section applies to, unless the divergence is intentional: the runtime file for each var is kept identical to .env.example apart from its value.\"}}';; esac; } 2>/dev/null || true",
            "statusMessage": "Checking .env sync"
          }
        ]
      }
    ]
  }
}
